name = "sparkjar"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "dist"

# nodejs_compat is load-bearing: api/_lib/store.js and api/_lib/auth/apple.js use
# node:crypto (randomBytes, createHash, createPublicKey) plus jsonwebtoken and
# bcryptjs. With the flag they run unmodified, which is why no auth code had to
# be rewritten for this migration.

[vars]
ALLOWED_ORIGIN = "https://sparkjar.heyitsmejosh.com"
APP_URL = "https://sparkjar.heyitsmejosh.com"
# Not a secret: native Sign in with Apple sets the token `aud` to the BUNDLE ID,
# and iOS + macOS both ship com.heyitsmejosh.spark. Without it api/_lib/auth/apple.js
# 500s with "Apple Sign In is misconfigured" — which is exactly the error App Review
# hit at step 3 of the 2.1(a) rejection. Keep it in vars so it survives redeploys.
APPLE_CLIENT_ID = "com.heyitsmejosh.spark"

# Secrets are NOT set here. Set them with:
#   npx wrangler pages secret put <NAME> --project-name sparkjar
# Required: JWT_SECRET, SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY
# Optional: RESEND_API_KEY, MAIL_FROM, STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET,
#           STRIPE_PRICE_ID, GITHUB_CLIENT_ID, GITHUB_CLIENT_SECRET,
#           GEMMA_KEY, SPARK_DAEMON_SECRET
# APPLE_CLIENT_ID is NOT optional while the apps ship a Sign in with Apple button;
# it lives in [vars] above.

[ai]
binding = "AI"

# Cloudflare Email Sending would remove the last API key from this project, but
# a Pages config REJECTS a [[send_email]] binding outright (same wall dream hit
# with its ratelimit binding). Adding it fails config validation at deploy.
# api/_lib/mail.js already prefers a globalThis.__env.EMAIL binding if one ever
# appears, so the remaining work is either attaching it in the Pages dashboard
# or moving this project to a Worker with [assets]. Until then mail needs a live
# RESEND_API_KEY, and password reset returns an honest 503 without one.
