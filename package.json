{
  "name": "sparkjar",
  "version": "2.2.0",
  "description": "Idea-sharing platform with filters, voting, auth",
  "keywords": ["idea-sharing", "voting", "forum"],
  "main": "index.html",
  "scripts": {
    "test": "vitest run",
    "build": "bash scripts/build-static.sh",
    "dev": "npm run build && npx wrangler pages dev dist",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name sparkjar"
  },
  "author": "Joshua Trommel",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "jsonwebtoken": "^9.0.3",
    "stripe": "^22.4.0"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
